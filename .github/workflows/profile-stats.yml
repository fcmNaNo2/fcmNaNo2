name: Update profile stats

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Generate stats card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: stats
          options: 'username=fcmNaNo2&custom_title=GitHub%20Stats&show_icons=true&hide_rank=true&locale=cn&theme=transparent&hide_border=true&text_bold=false&title_color=2563eb&text_color=475569&icon_color=2563eb'
          path: profile/stats.svg
          token: ${{ secrets.GITHUB_TOKEN }}
          fail_on_error: true

      - name: Generate top languages card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: top-langs
          options: 'username=fcmNaNo2&layout=compact&langs_count=5&locale=cn&theme=transparent&hide_border=true&text_bold=false&title_color=2563eb&text_color=475569'
          path: profile/top-langs.svg
          token: ${{ secrets.GITHUB_TOKEN }}
          fail_on_error: true

      - name: Commit profile cards
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add profile/stats.svg profile/top-langs.svg
          git commit -m 'chore: update profile stats' || exit 0
          git push
